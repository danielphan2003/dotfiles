#!/usr/bin/env -S bash -l
#
# Script to set colors generated by 'wal'
# https://github.com/dylanaraps/pywal

feh --bg-fill "$(< "${HOME}/.cache/wal/wal")" &
dunstwal &
xrdb -merge ${HOME}/.cache/wal/rofi.xres &
${HOME}/.config/bspwm/colors &
spicetify update

